# SkillGuard-SONIC 分阶段任务计划

## 项目核心定位

本项目基于 **SONIC / GEAR-SONIC** 这类 humanoid behavior foundation model，研究长时程人形导览任务中的安全技能编排问题。

核心观点：

> SONIC 负责“动作能不能做”；SkillGuard-SONIC 负责“现在该不该做、什么时候做、怎么安全切换、失败后怎么恢复”。

项目目标是在 Isaac Lab / Isaac Sim 中完成人形导览任务：

```text
walk → stop → face user → gesture / point → continue walking → recover if unsafe
```

重点评估指标：

```text
invalid switch
switch-induced failure
unsafe expressive motion
near-fall / fall
recovery success
long-horizon task success
```

---

# 阶段 0：确定论文边界和最小故事

## 目标

先把论文故事定死：

> 基于 SONIC 的 humanoid behavior foundation model，研究长时程导览任务中的安全技能编排问题。

本文不主张训练一个更强的 whole-body controller，而是解决：

```text
SONIC 能做很多动作，
但它不知道什么时候该做、什么时候不该做、什么时候要先稳定、失败后怎么恢复。
```

## 当前阶段要完成的东西

写一页 proposal，固定以下内容：

```text
主基座：SONIC / GEAR-SONIC
主方法：SkillGuard-SONIC
主要对比：SONIC-Naive, SONIC-FSM, Switch-style, HOVER
主要任务：skill-switch stress test + flat guide task + hazard-aware gesture task
主要指标：invalid switch, switch-induced failure, unsafe gesture, task success
```

## 成功标准

能够用一句话讲清楚：

> SkillGuard-SONIC 是 SONIC 之上的任务级安全技能编排层，用 contract、risk estimator、transition preparation 和 recovery 解决长时程导览任务中的危险切换问题。

---

# 阶段 1：跑通 SONIC 原始代码

## 目标

不要急着加 SkillGuard。第一阶段只做一件事：

> 确认 SONIC / GEAR-SONIC 能在仿真中跑起来。

## 要做的事情

基于：

```text
NVlabs/GR00T-WholeBodyControl
```

先跑通官方 demo / eval / sim2sim 流程。

需要确认能拿到这些信息：

```text
base velocity
torso pitch / roll
joint states
foot contact
tracking error
fall flag
collision flag
当前执行的 motion / command / skill
```

同时确认 SONIC 能执行几个基本动作：

```text
walk
stop
turn
gesture / upper-body motion
point / face direction
```

## 产出

```text
SONIC 能跑
能控制机器人
能记录状态
能切换至少两个动作
能保存 episode log
```

## 成功标准

最小成功标准：

```text
机器人可以 walk → stop → gesture → walk
并且你能记录整个过程中的状态和是否失败
```

这一步完成后，才真正进入 SkillGuard 实现阶段。

---

# 阶段 2：实现 SONIC-Naive baseline

## 目标

建立第一个 baseline：

> 任务到了哪个阶段，就直接让 SONIC 执行对应动作。

例如：

```text
到达展品 → stop
stop 之后 → face user
face user 之后 → gesture
gesture 完成 → walk to next goal
```

不加任何安全判断。

## 要做的事情

实现一个简单 task script：

```text
WalkSkill
StopSkill
GestureSkill
WalkSkill
```

但此时这些还不是真正的 Skill，只是直接调用 SONIC command / motion。

## 产出

```text
SONIC-Naive baseline
基础任务脚本
基础日志系统
```

## 成功标准

可以跑一个最小 guide sequence：

```text
起点 → 目标点 A → 停下 → 做手势 → 目标点 B
```

即使它有时失败也没关系，因为失败正是后面要解决的问题。

---

# 阶段 3：建立 logging 和核心指标

## 目标

在加方法之前，先把实验指标建好。否则后面很难写论文。

## 每一步要记录

```text
time
current_skill
target_skill
switch_requested
switch_allowed
switch_rejected_reason
base_velocity
torso_pitch
torso_roll
foot_contact
tracking_error
distance_to_obstacle
distance_to_edge
fall_flag
near_fall_flag
collision_flag
recovery_triggered
```

## 每个 episode 要汇总

```text
task_success
completion_time
number_of_switches
invalid_switch_attempts
rejected_switches
switch_induced_failures
unsafe_gestures
near_falls
falls
collisions
recovery_success
```

## 定义三个核心指标

### Invalid Switch

目标 skill 的条件不满足，但系统仍然执行切换。

例如：

```text
速度太快时切 gesture
身体不稳时切 point
靠近边缘时执行大幅上身动作
```

### Switch-Induced Failure

切换后 1 到 2 秒内发生：

```text
fall
near-fall
foot slip
collision
tracking divergence
emergency stop
```

### Unsafe Gesture

在危险状态下执行 expressive motion，例如：

```text
高速移动中 gesture
靠近边缘 gesture
姿态不稳 gesture
双脚接触不稳定 gesture
```

## 成功标准

跑 SONIC-Naive 时，能够自动输出：

```text
task success rate
invalid switch count
switch-induced failure count
unsafe gesture count
fall / near-fall count
```

这一步非常关键。没有这些指标，SkillGuard 的贡献会说不清楚。

---

# 阶段 4：实现 Skill wrapper 和 Skill Contract

## 目标

把 SONIC 的动作接口包装成真正的 skill。

核心抽象：

```text
Skill = SONIC action interface
      + precondition
      + invariant
      + termination
      + failure detector
      + recovery hook
```

## 先实现 4 个最小 skill

第一版只做：

```text
WalkSkill
StopSkill
GestureSkill
RecoverySkill
```

不要一开始做太多。

## 每个 skill 的接口

```python
class Skill:
    def precondition(self, obs, world_state):
        pass

    def enter(self, obs, world_state):
        pass

    def act(self, obs, command):
        pass

    def monitor(self, obs, world_state):
        pass

    def exit(self, obs, world_state):
        pass

    def recover(self, obs, world_state):
        pass
```

## GestureSkill 的 contract 示例

```text
允许进入：
base velocity < threshold
torso pitch / roll < threshold
双脚接触稳定
远离障碍物
远离边缘
tracking error 不高

执行中必须保持：
torso 不严重倾斜
foot contact 不异常
tracking error 不发散

失败后：
中止 gesture
进入 stop
必要时 recovery
```

## WalkSkill 的 contract 示例

```text
允许进入：
目标速度在 SONIC 安全范围内
前方没有 imminent collision
地形可通行

失败条件：
碰撞
严重倾斜
tracking divergence
```

## StopSkill

```text
基本总是允许
作为 fallback skill
```

## RecoverySkill

```text
在 unsafe 状态下触发
用于恢复站稳或进入安全状态
```

## 产出

```text
SkillGuard w/o Risk
```

也就是只有 contract，没有 learned risk estimator。

## 成功标准

系统可以做到：

```text
当速度太快时，拒绝 Walk → Gesture
当姿态不稳时，拒绝 Gesture
当检测到 near-fall 时，进入 Recovery
```

---

# 阶段 5：实现基础对比方法

## 目标

建立主要 baselines，让后面的实验可以直接比较。

## Baseline 1：SONIC-Naive

任务阶段到了就直接切。

```text
不检查速度
不检查姿态
不检查接触
不检查环境风险
```

作用：

```text
证明强 controller 不等于安全编排
```

## Baseline 2：SONIC-FSM

手写规则：

```text
if velocity high: wait
if torso unstable: stop
if near edge: no gesture
if stable: gesture
```

作用：

```text
证明 SkillGuard 不是简单 FSM
```

## Baseline 3：SONIC-Smooth

只做 command interpolation 或 smooth transition。

作用：

```text
证明 smooth transition 不等于 safe transition
```

## Baseline 4：Switch-style Scheduler

参考 Switch 的思想，做一个简化版本：

```text
建立 skill graph
给不同 skill transition 一个 cost
用 graph search 找中间 skill
优先选择 motion-level 更平滑的 transition
```

作用：

```text
证明 Switch-style 解决 motion-level transition，
但不完全解决 task-context safety
```

## Baseline 5：HOVER baseline

这个可以稍后做，不一定第一阶段就完成。

它作为上一代 multi-mode controller baseline：

```text
HOVER + naive mode switching
或 HOVER + FSM
```

作用：

```text
证明 SONIC 是更强基座；
但即使 SONIC 更强，也仍然需要 SkillGuard。
```

## 成功标准

至少跑通前三个：

```text
SONIC-Naive
SONIC-FSM
SkillGuard w/o Risk
```

Switch-style 和 HOVER 可以作为第二轮补充。

---

# 阶段 6：做第一组核心实验：Skill-Switch Stress Test

## 目标

先不做复杂导览场景，只测试 skill 切换是否安全。

这是最重要、最容易出结果的一组实验。

## 测试切换

```text
Walk → Stop
Walk → Gesture
Gesture → Walk
Walk → Point
Turn → Gesture
Stop → Walk
```

第一版可以只做：

```text
Walk → Gesture
Gesture → Walk
Walk → Stop
```

## 加入扰动

```text
base velocity 高低变化
torso pitch / roll 扰动
foot contact 不稳定
tracking error 增大
靠近障碍物
靠近平台边缘
低摩擦地面
```

## 对比方法

```text
SONIC-Naive
SONIC-Smooth
SONIC-FSM
SkillGuard w/o Risk
```

## 主要指标

```text
switch success rate
invalid switch attempts
switch-induced failure rate
near-fall rate
fall rate
tracking error after switch
```

## 你希望看到的结果

```text
SONIC-Naive：切换频繁，但危险切换多
SONIC-Smooth：动作更平滑，但仍然会在错误状态下切换
SONIC-FSM：比 Naive 稳，但规则僵硬
SkillGuard w/o Risk：显著减少 invalid switch 和 switch-induced failure
```

## 成功标准

能画出第一张核心图：

```text
不同扰动强度下，SkillGuard 的 switch-induced failure 明显低于 baseline
```

---

# 阶段 7：训练 Switch-Risk Estimator

## 目标

把 SkillGuard 从“规则系统”升级成“risk-aware 方法”。

这是让创新更够 ICRA 的关键阶段。

## 数据怎么收集

用 SONIC-Naive 和随机扰动生成大量切换样本。

每个样本记录：

```text
source_skill
target_skill
base_velocity
torso_pitch / roll
foot_contact
tracking_error
distance_to_obstacle
distance_to_edge
terrain_friction
command_magnitude
```

标签是：

```text
切换后 1-2 秒内是否发生 failure
```

failure 包括：

```text
near-fall
fall
collision
foot slip
tracking divergence
emergency stop
```

## 模型先用简单的

不要一开始做复杂 neural network。

优先顺序：

```text
logistic regression
random forest
small MLP
```

## 风险模型输出

```text
P(switch-induced failure | state, source_skill, target_skill)
```

## 接入 SkillGuard

切换逻辑变成：

```text
contract satisfied?
    no → reject / prepare / recover
    yes → check risk

risk < threshold?
    yes → switch
    no → prepare / wait / stop / recover
```

## 产出

```text
Full SkillGuard-SONIC
```

## 成功标准

证明：

```text
SkillGuard + learned risk
比纯手写 contract 更少发生 switch-induced failure
同时不会过度保守
```

---

# 阶段 8：做第二组实验：Flat Guide Task

## 目标

证明方法不只是单次切换有效，而是在长时程导览任务中有效。

## 任务设计

```text
机器人从入口出发
走到展品 A
停下
面向用户
指向展品 / 做讲解动作
继续走到展品 B
停下
讲解
最后到达终点
```

## 使用 skills

```text
WalkSkill
StopSkill
FaceUserSkill
GestureSkill
RecoverySkill
```

第一版没有 FaceUserSkill 也可以，先用 GestureSkill 代替。

## 对比方法

```text
SONIC-Naive
SONIC-FSM
Switch-style Scheduler
SkillGuard w/o Risk
Full SkillGuard
```

## 指标

```text
task success
completion time
invalid switch
switch-induced failure
unsafe gesture
near-fall
fall
recovery success
number of rejected switches
```

## 你希望看到的结果

```text
Naive 成功率低，unsafe gesture 多
FSM 较稳，但容易过度保守或失败恢复差
Switch-style 平滑，但在环境风险下仍可能 unsafe
SkillGuard 成功率最高，unsafe gesture 和 switch-induced failure 最少
```

## 成功标准

得到一张主结果表：

```text
Full SkillGuard 在 task success 上最高，
在 switch-induced failure / unsafe gesture 上最低。
```

---

# 阶段 9：做第三组实验：Hazard-Aware Gesture Task

## 目标

突出项目特色：

> 自然动作不是任何时候都该做。

这和导览机器人非常契合。

## 场景设置

机器人尝试做 gesture / point / explanation motion，但当前状态不同：

```text
平地稳定站立
低速行走
高速行走
靠近障碍物
靠近平台边缘
姿态扰动后
低摩擦地面
foot contact 不稳定
```

## 任务

系统决定：

```text
现在执行 gesture
延迟 gesture
先 stop
先 recover
拒绝 gesture
```

## 对比方法

```text
SONIC-Naive
SONIC-FSM
Switch-style
SkillGuard
```

## 指标

```text
unsafe gesture rate
gesture completion rate
near-fall during gesture
waiting time before gesture
task success
motion tracking error
```

## 你希望看到的结果

```text
Naive：gesture 完成率高，但 unsafe gesture 多
FSM：unsafe gesture 少，但可能过度保守
SkillGuard：在危险时抑制，在稳定后恢复，整体 task success 更高
```

## 成功标准

展示一个很直观的案例：

```text
Baseline 在靠近边缘时直接 gesture，near-fall；
SkillGuard 拒绝 gesture，先后退 / stop / stabilize，再执行 gesture。
```

这会非常适合论文视频。

---

# 阶段 10：做鲁棒性和消融实验

## 目标

证明每个模块都有用。

## 消融 1：去掉 Risk Estimator

```text
SkillGuard w/o Risk
vs
Full SkillGuard
```

证明 learned risk 有用。

## 消融 2：去掉 Recovery

```text
SkillGuard w/o Recovery
vs
Full SkillGuard
```

证明 recovery 对 long-horizon task success 有用。

## 消融 3：去掉 Transition Preparation

```text
只拒绝危险切换
但不主动 slow down / stabilize
```

证明 preparation 不是可有可无。

## 消融 4：只用 FSM

```text
SONIC-FSM
vs
SkillGuard
```

证明 contract + risk + recovery 比全局规则更强。

## 鲁棒性变量

```text
速度扰动强度
姿态扰动强度
感知噪声强度
障碍物距离
边缘距离
摩擦系数
tracking error
```

## 关键曲线

```text
noise level vs task success
perturbation level vs switch-induced failure
edge distance vs unsafe gesture rate
risk threshold vs success / conservativeness
```

---

# 阶段 11：补充 HOVER 和 Switch 对比

## 目标

让论文的 related baseline 更完整。

## HOVER 对比

HOVER 作为上一代 multi-mode whole-body controller baseline。

可以做：

```text
HOVER-Naive
HOVER-FSM
SONIC-Naive
SkillGuard-SONIC
```

证明：

```text
SONIC 相比 HOVER 是更强基座；
SkillGuard 解决的是更高层 orchestration；
即使底层变强，危险切换问题仍然存在。
```

## Switch 对比

Switch-style 对比要强调互补关系。

可以做三组：

```text
SONIC + Switch-style
SONIC + SkillGuard
SONIC + Switch-style + SkillGuard
```

预期结果：

```text
Switch-style：motion transition 更平滑
SkillGuard：task-context safety 更强
Switch + SkillGuard：最好
```

这会让 story 更稳，因为不是单纯说 Switch 不行，而是说：

> Switch 解决 motion-level transition；SkillGuard 解决 task-level safety gating。

---

# 阶段 12：整理论文结果和视频

## 目标

把实验变成论文材料。

## 必备图

### 图 1：系统架构

```text
Task Goal
  ↓
SkillGuard Router
  ↓
Contract Checker + Risk Estimator
  ↓
Transition Preparation / Recovery
  ↓
SONIC Controller
  ↓
Humanoid in Isaac Sim
```

### 图 2：Skill timeline

展示一个 episode：

```text
Walk → Stop → FaceUser → Gesture → Walk → Recovery → Walk
```

并标出：

```text
rejected unsafe switch
risk score
recovery trigger
successful retry
```

### 图 3：主结果表

比较：

```text
SONIC-Naive
SONIC-FSM
Switch-style
SkillGuard w/o Risk
SkillGuard w/o Recovery
Full SkillGuard
```

### 图 4：鲁棒性曲线

```text
扰动越强，baseline 掉得越快；
SkillGuard 更稳。
```

### 图 5：失败案例

展示 baseline 失败，SkillGuard 成功。

## 视频必须展示

```text
Naive 直接 gesture 导致 near-fall
SkillGuard 先 stop / stabilize 再 gesture
Switch-style 平滑切换但忽略边缘风险
SkillGuard 拒绝 unsafe gesture
Full long-horizon guide task
```

---

# 推荐执行优先级

```text
第 1 优先级：
跑通 SONIC + logging

第 2 优先级：
实现 SONIC-Naive / FSM / SkillGuard w/o Risk

第 3 优先级：
做 skill-switch stress test

第 4 优先级：
训练 switch-risk estimator

第 5 优先级：
做 flat guide task

第 6 优先级：
做 hazard-aware gesture task

第 7 优先级：
补 Switch-style baseline

第 8 优先级：
补 HOVER baseline

第 9 优先级：
做消融、鲁棒性、视频
```

---

# 最小可投稿版本

最小版本做到下面这些就可以形成初稿：

```text
Method:
  SkillGuard-SONIC
  contract + risk estimator + recovery

Baselines:
  SONIC-Naive
  SONIC-FSM
  Switch-style
  SkillGuard w/o Risk

Tasks:
  skill-switch stress test
  flat guide task
  hazard-aware gesture task

Metrics:
  task success
  invalid switch
  switch-induced failure
  unsafe gesture
  fall / near-fall
```

HOVER baseline 可以作为加分项，不要让它阻塞主线。

---

# 一句话总结

你的推进路线应该是：

> 先跑通 SONIC，再把动作包装成 skill；先用 contract 做安全切换，再用 rollout 学 switch-risk；先做单次切换 stress test，再做长时程导览任务；最后补 Switch-style 和 HOVER 对比。
